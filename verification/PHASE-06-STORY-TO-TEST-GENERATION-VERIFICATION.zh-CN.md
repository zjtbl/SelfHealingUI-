# PHASE-06-STORY-TO-TEST-GENERATION-VERIFICATION（中文）

## 必须检查项
1. `S06-01` 故事卡输入被标准化为结构化验收标准。
   证据：story normalization 产物。
2. `S06-02` 先生成 BDD 草稿，再生成 Java 草稿。
   证据：生成的 `.feature` 草稿产物。
3. `S06-03` 包含已有 step 复用分析。
   证据：step 复用报告。
4. `S06-04` 人工审查门禁阻断自动合并。
   证据：评审工作流记录。

## 通过规则
四项全部通过。

## 审核角色
BDD 架构负责人。
