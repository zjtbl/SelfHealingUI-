# PHASE-02-RUNTIME-INVENTORY（中文）

## 目标
采集运行态 UI 证据并建立可复用元素画像。

## 输入
- 目标路由
- 登录会话与种子账号
- 浏览器执行配置

## 输出
- 运行态快照（DOM、a11y、截图）
- 含语义 ID 的元素画像数据
- 版本化证据引用

## 准出标准
- 关键可交互元素具备稳定语义 ID。
- 快照产物可按 route 和 run ID 查询。

## 非本阶段范围
- 跨项目共享与长期留存策略优化。
