# TEST_STRATEGY.md（中文）

## 策略目标
确保自愈变更具备可信性、可复现性、可审查性。

## V1 执行策略
1. V1 默认是“仅分析模式”（不写补丁）。
2. V1.5 写分支流需要按项目显式开启开关。
3. 开启写分支流后，可自动触发分支 CI。
4. 合并主干始终保持人工触发。

## 验证分层
1. 静态检查
2. 运行态 UI 验证
3. 补丁安全检查
4. 场景重跑检查
5. 治理检查

## 静态检查
- locator 清单提取完整性
- 前端 diff 解析完整性
- 阶段配置 schema 合法性

## 运行态 UI 验证
- 候选 locator 唯一性
- 可见性与可操作性
- 动作回放通过（click/fill/assert）
- AntD 浮层组件的 scope 正确性

## 补丁安全
- 最小 diff 策略
- 可编辑范围策略
- 禁止弱化断言
- 禁止业务步骤语义漂移

## 重跑验证
- 单场景重跑通过
- 可选受影响集合重跑通过
- 证据包完整

## 治理验证
- 必须有 L1/L2/L3 分类
- L2/L3 不得进入自动 locator 补丁流
- 每次运行记录必须具备审计字段
- 回退冲突必须打标并停止自动处理

## 准出规则
仅当 `verification/` 中对应阶段验证文件全部满足时，阶段才算通过。
